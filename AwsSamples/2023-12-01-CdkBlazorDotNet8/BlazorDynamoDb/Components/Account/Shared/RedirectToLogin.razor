@using Common.Constants
@inject NavigationManager NavigationManager

@code {

  protected override void OnInitialized()
  {
    NavigationManager.NavigateTo($"{AppPath.AccountLogin}?returnUrl={Uri.EscapeDataString(NavigationManager.Uri)}", true);
  }

}
