{
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "dotnet tool restore",
    "build": "dotnet fable ./src && vite build",
    "start": "dotnet fable watch ./src --run vite --open",
    "serve": "vite preview --open"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "fable-compiler": "^2.3.25",
    "fable-loader": "^2.1.8",
    "vite": "^5.2.11"
  }
}
